<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>vue基础知识点总结(一)</title>
    <url>/2020/08/01/vue%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9%E6%80%BB%E7%BB%93(%E4%B8%80)/</url>
    <content><![CDATA[<h3 id="创建一个vue实例，Vue是一个构造函数"><a href="#创建一个vue实例，Vue是一个构造函数" class="headerlink" title="创建一个vue实例，Vue是一个构造函数"></a>创建一个vue实例，Vue是一个构造函数</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!--控制的这个元素区域就是mvvm中的v--&gt;</span><br><span class="line">&lt;div id&#x3D;&quot;app&quot;&gt;</span><br><span class="line">    &lt;p&gt;&#123;&#123;msg&#125;&#125;&lt;&#x2F;p&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">    &#x2F;&#x2F; new出来的这个vm对象就是mvvm中的vm</span><br><span class="line">    let vm &#x3D; new Vue(&#123;</span><br><span class="line">        el: &quot;#app&quot;, &#x2F;&#x2F; 表示当前我们new的这个Vue实例,要控制页面上的哪个区域</span><br><span class="line">        &#x2F;&#x2F; data就是mvvm中的m</span><br><span class="line">        data: &#123;</span><br><span class="line">            msg: &quot;hello world!&quot;    </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="Vue指令"><a href="#Vue指令" class="headerlink" title="Vue指令"></a>Vue指令</h3><h4 id="v-cloak"><a href="#v-cloak" class="headerlink" title="v-cloak"></a>v-cloak</h4><p>解决网络不好情况下用户看到插值表达式的情况，解决闪烁的问题</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;style&gt;</span><br><span class="line">    [v-cloak]&#123;</span><br><span class="line">        display: none;</span><br><span class="line">    &#125;</span><br><span class="line">&lt;&#x2F;style&gt;</span><br><span class="line">&lt;P v-cloak&gt;&#123;&#123;msg&#125;&#125;&lt;&#x2F;p&gt;</span><br></pre></td></tr></table></figure>

<h4 id="v-text"><a href="#v-text" class="headerlink" title="v-text"></a>v-text</h4><p>默认v-text是没有闪烁问题的，但是会覆盖原有的内容，插值表达式不会覆盖内容，但有闪烁问题，这是这两者的区别，两者也都不能解析html标签</p>
<h4 id="v-bind"><a href="#v-bind" class="headerlink" title="v-bind"></a>v-bind</h4><p>Vue提供用于绑定属性的指令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">v-bind:title&#x3D;&quot;mytitle&quot; 简写&#x3D;&gt; :title&#x3D;&quot;mytitle&quot;  </span><br></pre></td></tr></table></figure>

<h4 id="v-on"><a href="#v-on" class="headerlink" title="v-on"></a>v-on</h4><p>Vue提供了 v-on 事件绑定机制</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">v-on:click&#x3D;&quot;show&quot; 简写&#x3D;&gt; @click&#x3D;&quot;show&quot;</span><br></pre></td></tr></table></figure>

<h4 id="事件修饰符"><a href="#事件修饰符" class="headerlink" title="事件修饰符"></a>事件修饰符</h4><p>.stop          阻止冒泡<br>.prevent    阻止默认事件<br>.self          只当在 event.target 是当前元素自身时触发处理函数，即事件不是从内部元素触发的<br>.once        点击事件将只会触发一次<br>.passive    滚动事件的默认行为 (即滚动行为) 将会立即触发，而不会等待 <code>onScroll</code> 完成<br>.stop 和 .self的区别<br>.stop        阻止了所有的冒泡,.self只负责自己的冒泡</p>
<h4 id="v-model-双向数据绑定"><a href="#v-model-双向数据绑定" class="headerlink" title="v-model 双向数据绑定"></a>v-model 双向数据绑定</h4><p>v-bind 只能实现数据的单向数据绑定,从m自动绑定到v，无法实现数据的双向数据绑定<br>使用v-model 指令，可以实现表单元素和model中数据的双向数据绑定<br>注意v-model 只能运用在表单元素中<br>input(radio,text,address,email…) select checkbox textarea</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;input type&#x3D;&quot;text&quot; v-model:value&#x3D;&quot;msg&quot;&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">let vm &#x3D; new Vue(&#123;</span><br><span class="line">    el:&quot;#app&quot;,  </span><br><span class="line">    data:&#123;  </span><br><span class="line">        msg:&quot;Vue&quot;,</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h4 id="Vue中的-class-绑定"><a href="#Vue中的-class-绑定" class="headerlink" title="Vue中的 class 绑定"></a>Vue中的 class 绑定</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;style&gt;</span><br><span class="line">    .red&#123;</span><br><span class="line">        color:red;</span><br><span class="line">    &#125;</span><br><span class="line">    .green&#123;</span><br><span class="line">        color:green;</span><br><span class="line">    &#125;</span><br><span class="line">    .active&#123;</span><br><span class="line">        color:blue;</span><br><span class="line">    &#125;</span><br><span class="line">&lt;&#x2F;style&gt;</span><br><span class="line">&lt;!--第一种使用方式，直接传递一个数组--&gt;</span><br><span class="line">&lt;h1 :class&#x3D;&quot;[&#39;red&#39;,&#39;green&#39;]&quot;&gt;asd&lt;&#x2F;h1&gt; </span><br><span class="line">&lt;!--第二种可以在数组中使用三元表达式--&gt;</span><br><span class="line">&lt;h1 :class&#x3D;&quot;[&#39;red&#39;,&#39;green&#39;,flag?&#39;active&#39;:&#39;&#39;]&quot;&gt;asd&lt;&#x2F;h1&gt;</span><br><span class="line">&lt;!--第三种在数组里面写对象--&gt;</span><br><span class="line">&lt;h1 :class&#x3D;&quot;[&#39;red&#39;,&#39;green&#39;,&#123;&#39;active&#39;:flag&#125;]&quot;&gt;asd&lt;&#x2F;h1&gt;</span><br><span class="line">&lt;script&gt; </span><br><span class="line">let vm &#x3D; new Vue(&#123;</span><br><span class="line">    el:&#39;#app&#39;,</span><br><span class="line">    data:&#123;</span><br><span class="line">        flag: false</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h4 id="Vue中的-style-内联样式绑定"><a href="#Vue中的-style-内联样式绑定" class="headerlink" title="Vue中的 style 内联样式绑定"></a>Vue中的 style 内联样式绑定</h4><p><code>v-bind:style</code> 的对象语法十分直观——看着非常像 CSS，但其实是一个 JavaScript 对象。CSS property 名可以用驼峰式 (camelCase) 或短横线分隔 (kebab-case，记得用引号括起来) 来命名：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div v-bind:style&#x3D;&quot;&#123; color: activeColor, fontSize: fontSize + &#39;px&#39; &#125;&quot;&gt;&lt;&#x2F;div&gt;</span><br><span class="line">data: &#123;</span><br><span class="line">  activeColor: &#39;red&#39;,</span><br><span class="line">  fontSize: 30</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>直接绑定到一个样式对象通常更好，这会让模板更清晰：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div v-bind:style&#x3D;&quot;styleObject&quot;&gt;&lt;&#x2F;div&gt;</span><br><span class="line">data: &#123;</span><br><span class="line">  styleObject: &#123;</span><br><span class="line">    color: &#39;red&#39;,</span><br><span class="line">    fontSize: &#39;13px&#39;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当 <code>v-bind:style</code> 使用需要添加浏览器引擎前缀的 CSS property 时，如 <code>transform</code>，Vue.js 会自动侦测并添加相应的前缀</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div :style&#x3D;&quot;&#123; display: [&#39;-webkit-box&#39;, &#39;-ms-flexbox&#39;, &#39;flex&#39;] &#125;&quot;&gt;&lt;&#x2F;div&gt;</span><br></pre></td></tr></table></figure>

<h4 id="v-for-循环"><a href="#v-for-循环" class="headerlink" title="v-for 循环"></a>v-for 循环</h4><p>在使用v-for循环数组或对象的时候，记得绑定 :key 唯一标识，数组用 of 分隔符，对象用 in 分隔符</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">循环数组</span><br><span class="line">&lt;p v-for&#x3D;&quot; (item,index) of list&quot; :key&#x3D;&quot;index&quot;&gt;</span><br><span class="line">    &#123;&#123;index&#125;&#125;-&#123;&#123;item.message&#125;&#125;</span><br><span class="line">&lt;&#x2F;p&gt;</span><br><span class="line">循环对象</span><br><span class="line">&lt;p v-for&#x3D;&quot;(item,name,index) in listObject&quot; :key&#x3D;&quot;index&quot;&gt;</span><br><span class="line">    &#123;&#123;index&#125;&#125;-&#123;&#123;name&#125;&#125;-&#123;&#123;item&#125;&#125;</span><br><span class="line">&lt;&#x2F;p&gt;</span><br><span class="line">&lt;!--key的使用注意事项</span><br><span class="line">v-for循环的时候 key属性只能使用 number或string</span><br><span class="line">key在使用的时候，必须使用v-bind属性绑定的形式，指定key的值</span><br><span class="line">在组件中，使用v-for循环的时候，或者在一些特殊情况中，</span><br><span class="line">如果v-for有问题必须在使用v-for的同时，指定唯一的字符串&#x2F;数字类型:key值</span><br><span class="line">留个问题：为什么要绑定 key 呢？ 嘿嘿--&gt;</span><br></pre></td></tr></table></figure>

<h4 id="v-if-和-v-show"><a href="#v-if-和-v-show" class="headerlink" title="v-if 和 v-show"></a>v-if 和 v-show</h4><p>v-if 的特点是每次都会删除或创建元素<br>v-show 的特点每次不会重新进行dom的删除和创建，只是切换了display的样式<br>v-if 有较高的切换性能消耗<br>v-show 有较高的初始渲染消耗<br>如果元素涉及到频繁的切换不要使用 v-if ，推荐使用 v-show<br>如果元素可能永远不会被显示出来被看到，推荐使用 v-if</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;button @click&#x3D;&quot;toggle&quot; type&#x3D;&quot;button&quot;&gt;切换&lt;&#x2F;button&gt;</span><br><span class="line">&lt;h3 v-if&#x3D;&quot;flag&quot;&gt;这是v-if控制的元素&lt;&#x2F;h3&gt;</span><br><span class="line">&lt;h3 v-show&#x3D;&quot;flag&quot;&gt;这是v-show控制的元素&lt;&#x2F;h3&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">    let vm &#x3D; new Vue(&#123;</span><br><span class="line">        el:&#39;#app&#39;,</span><br><span class="line">        data:&#123;</span><br><span class="line">            flag:false</span><br><span class="line">        &#125;,</span><br><span class="line">        methods:&#123;</span><br><span class="line">             toggle()&#123;</span><br><span class="line">                this.flag &#x3D; !this.flag</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="Vue-计算属性、监听、方法"><a href="#Vue-计算属性、监听、方法" class="headerlink" title="Vue 计算属性、监听、方法"></a>Vue 计算属性、监听、方法</h3><h4 id="1、computed-计算属性是有缓存机制的，如果涉及到的值没有改变，不会重新计算，页面不会重新渲染，可以监听多个变量。"><a href="#1、computed-计算属性是有缓存机制的，如果涉及到的值没有改变，不会重新计算，页面不会重新渲染，可以监听多个变量。" class="headerlink" title="1、computed:计算属性是有缓存机制的，如果涉及到的值没有改变，不会重新计算，页面不会重新渲染，可以监听多个变量。"></a>1、computed:计算属性是有缓存机制的，如果涉及到的值没有改变，不会重新计算，页面不会重新渲染，可以监听多个变量。</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div id&#x3D;&quot;app&quot;&gt;</span><br><span class="line">    &lt;input type&#x3D;&quot;text&quot; v-model&#x3D;&quot;firstname&quot;&gt; + </span><br><span class="line">    &lt;input type&#x3D;&quot;text&quot; v-model&#x3D;&quot;lastname&quot;&gt; &#x3D; </span><br><span class="line">    &lt;input type&#x3D;&quot;text&quot; v-model&#x3D;&quot;fullname&quot;&gt;</span><br><span class="line"> &lt;&#x2F;div&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">    var vm &#x3D; new Vue(&#123;</span><br><span class="line">        el: &#39;#app&#39;,</span><br><span class="line">        data: &#123;</span><br><span class="line">            firstname: &#39;&#39;,</span><br><span class="line">            lastname: &#39;&#39;,</span><br><span class="line">            fullname: &#39;&#39;</span><br><span class="line">        &#125;,</span><br><span class="line">        methods: &#123;&#125;,</span><br><span class="line">        computed: &#123; &#x2F;&#x2F;在computed中可以定义一些属性，这些属性叫做【计算属性】，</span><br><span class="line">            &#x2F;&#x2F;计算属性的本质就是一个方法，只不过在使用的时候，直接当作属性来使用</span><br><span class="line">            &#x2F;&#x2F;注意1，计算属性在引用的时候，一定不要加()去调用，直接把它当作普通属性区使用就好</span><br><span class="line">            &#x2F;&#x2F;注意2，只要计算属性这个function内部，所用到的任何data中的数据发生了变化，就会立即重新计算这个计算属性的值</span><br><span class="line">            &#x2F;&#x2F;注意3，计算属性的求值结果，会被缓存起来，方便下次直接使用；如果计算属性方法中，所有来的任何数据都没有发生变化则不会重新计算属性求值</span><br><span class="line">            &#39;fullname&#39;:function()&#123;</span><br><span class="line">                return this.firstname + &#39;-&#39; + this.lastname;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;);</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h4 id="2、watch-监听也是有缓存机制的，每次只能监听单个属性的变化，代码过于麻烦，如果三种都可以实现，建议使用计算属性。"><a href="#2、watch-监听也是有缓存机制的，每次只能监听单个属性的变化，代码过于麻烦，如果三种都可以实现，建议使用计算属性。" class="headerlink" title="2、watch:监听也是有缓存机制的，每次只能监听单个属性的变化，代码过于麻烦，如果三种都可以实现，建议使用计算属性。"></a>2、watch:监听也是有缓存机制的，每次只能监听单个属性的变化，代码过于麻烦，如果三种都可以实现，建议使用计算属性。</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;div id&#x3D;&quot;app&quot;&gt;</span><br><span class="line">        &lt;input type&#x3D;&quot;text&quot; v-model&#x3D;&quot;firstname&quot;&gt; + </span><br><span class="line">        &lt;input type&#x3D;&quot;text&quot; v-model&#x3D;&quot;lastname&quot;&gt; &#x3D; </span><br><span class="line">        &lt;input type&#x3D;&quot;text&quot; v-model&#x3D;&quot;fullname&quot;&gt;</span><br><span class="line">    &lt;&#x2F;div&gt;</span><br><span class="line">&lt;&#x2F;body&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">    const vm &#x3D; new Vue(&#123;</span><br><span class="line">        el: &#39;#app&#39;,</span><br><span class="line">        data: &#123;</span><br><span class="line">            firstname: &#39;&#39;,</span><br><span class="line">            lastname: &#39;&#39;,</span><br><span class="line">            fullname: &#39;&#39;</span><br><span class="line">        &#125;,</span><br><span class="line">        methods: &#123;</span><br><span class="line">            </span><br><span class="line">        &#125;,</span><br><span class="line">        watch: &#123; &#x2F;&#x2F; watch监听器vm实例属性，可以监听data中指定是数据的变化，然后触发这个watch中对应的处理函数</span><br><span class="line">            firstname:function(newVal,odlVal)&#123; </span><br><span class="line">                &#x2F;&#x2F;this.fullname &#x3D; this.firstname + &#39;-&#39; + this.lastname</span><br><span class="line">                this.fullname &#x3D; newVal + &#39;-&#39; + this.lastname</span><br><span class="line">            &#125;,</span><br><span class="line">            lastname:function(newVal)&#123;</span><br><span class="line">                this.fullname &#x3D; this.firstname + &#39;-&#39; + newVal</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h5 id="watch-监听复杂数据类型-object-array-深度监视"><a href="#watch-监听复杂数据类型-object-array-深度监视" class="headerlink" title="watch 监听复杂数据类型 object array 深度监视"></a>watch 监听复杂数据类型 object array 深度监视</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script&gt; </span><br><span class="line">watch:&#123;</span><br><span class="line">    msg:function(newVal,oldVal)&#123;</span><br><span class="line">        console.log(newVal,oldVal)</span><br><span class="line">    &#125;,</span><br><span class="line">    &#x2F;&#x2F;监听复杂数据类型 object array 深度监视</span><br><span class="line">    stus:&#123;</span><br><span class="line">        deep:true,&#x2F;&#x2F;深度监听</span><br><span class="line">        handler:function(newV,oldV)&#123;</span><br><span class="line">            console.log(newV[0].name)</span><br><span class="line">         &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h4 id="3、methods-方法没有缓存机制，每次都要重新渲染页面。"><a href="#3、methods-方法没有缓存机制，每次都要重新渲染页面。" class="headerlink" title="3、methods:方法没有缓存机制，每次都要重新渲染页面。"></a>3、methods:方法没有缓存机制，每次都要重新渲染页面。</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div id&#x3D;&quot;app&quot;&gt;</span><br><span class="line">    &lt;input type&#x3D;&quot;button&quot; value&#x3D;&quot;点击&quot; @click&#x3D;&quot;show&quot;&gt; &lt;!-- 调用show --&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">var vm &#x3D; new Vue(&#123; </span><br><span class="line">    el: &quot;#app&quot;,</span><br><span class="line">    data:&#123;&#125;,</span><br><span class="line">    methods:&#123;  &#x2F;&#x2F;这个methods属性中定义了当前vm实例所有可用的方法</span><br><span class="line">        show:function()&#123;</span><br><span class="line">            alert(&#39;hello&#39;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h4 id="4、如果涉及到vue实例外的数据发生变化，computed正常是不会重新渲染页面，但是如果watch监听里的变量如果改变，会导致computed重新计算渲染，单独修改arr值是不会发生变化，当watch监听到的值发生变化是，computed会重新计算。"><a href="#4、如果涉及到vue实例外的数据发生变化，computed正常是不会重新渲染页面，但是如果watch监听里的变量如果改变，会导致computed重新计算渲染，单独修改arr值是不会发生变化，当watch监听到的值发生变化是，computed会重新计算。" class="headerlink" title="4、如果涉及到vue实例外的数据发生变化，computed正常是不会重新渲染页面，但是如果watch监听里的变量如果改变，会导致computed重新计算渲染，单独修改arr值是不会发生变化，当watch监听到的值发生变化是，computed会重新计算。"></a>4、如果涉及到vue实例外的数据发生变化，computed正常是不会重新渲染页面，但是如果watch监听里的变量如果改变，会导致computed重新计算渲染，单独修改arr值是不会发生变化，当watch监听到的值发生变化是，computed会重新计算。</h4><h3 id="Vue-组件"><a href="#Vue-组件" class="headerlink" title="Vue 组件"></a>Vue 组件</h3><h4 id="1、全局组件"><a href="#1、全局组件" class="headerlink" title="1、全局组件"></a>1、全局组件</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">直接使用Vue.component创建组件</span><br><span class="line">Vue.component(&#39;mycom2&#39;,&#123;</span><br><span class="line">&#x2F;&#x2F; 组件的template属性指向的模板内容，必须有且只能有唯一的一个根元素</span><br><span class="line">    template:’&lt;h3&gt;这是直接使用Vue.component创建组件&lt;&#x2F;h3&gt;’</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h4 id="2、局部组件"><a href="#2、局部组件" class="headerlink" title="2、局部组件"></a>2、局部组件</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let 组件名 &#x3D; &#123;</span><br><span class="line">    props:[],</span><br><span class="line">    template:&quot;模板&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要在实例的 components 中挂载</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">components:&#123;</span><br><span class="line">     组件名(键):组件名(值)</span><br><span class="line">    &#x2F;&#x2F; 如果组件名键和值名字相同可简写为一个 组件名</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3、动态组件"><a href="#3、动态组件" class="headerlink" title="3、动态组件"></a>3、动态组件</h4><p>如果组件动态切换，可以用<component :is="属性名"></omponent>标签 ，is里面的属性名会查找对应的组件来切换。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; component是一个占位符, :is属性可以用来指定要展示的组件的名称</span><br><span class="line">&lt;a href&#x3D;&quot;&quot; @click&#x3D;&quot;comName&#x3D;&#39;login&#39;&quot;&gt;登录&lt;&#x2F;a&gt;</span><br><span class="line">&lt;a href&#x3D;&quot;&quot; @click&#x3D;&quot;comName&#x3D;&#39;register&#39;&quot;&gt;注册&lt;&#x2F;a&gt;</span><br><span class="line">&lt;component :is&#x3D;&quot;comName&quot;&gt;&lt;&#x2F;component&gt;</span><br><span class="line">Vue.component(&#39;login&#39;,&#123;</span><br><span class="line">    template:&#39;&lt;h3&gt;登录组件&lt;&#x2F;h3&gt;&#39;</span><br><span class="line">&#125;)</span><br><span class="line">Vue.component(&#39;register&#39;,&#123;</span><br><span class="line">    template:&#39;&lt;h3&gt;注册组件&lt;&#x2F;h3&gt;&#39;</span><br><span class="line">&#125;)</span><br><span class="line">data:&#123;</span><br><span class="line">    comName:&#39;&#39; &#x2F;&#x2F;当前显示的组件名称</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="组件传值"><a href="#组件传值" class="headerlink" title="组件传值"></a>组件传值</h4><h5 id="父传子-props"><a href="#父传子-props" class="headerlink" title="父传子    props"></a>父传子    props</h5><p>父组件，可以在引用组件的时候，通过属性绑定的形式，把需要传递给子组件的数据以属性绑定的形式，传递到子组件内部，供子组件使用。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div id&#x3D;&quot;app&quot;&gt;</span><br><span class="line">    &lt;com1 v-bind:parentmsg&#x3D;&quot;msg&quot;&gt;&lt;&#x2F;com1&gt; &lt;!-- 如果是动态的要在子组件上面绑定属性v-bind --&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">    let vm &#x3D; new Vue(&#123;</span><br><span class="line">        el : &#39;#app&#39;,</span><br><span class="line">        data:&#123;</span><br><span class="line">            msg:&#39;这是父组件中的数据&#39;</span><br><span class="line">        &#125;,</span><br><span class="line">        components&#123;</span><br><span class="line">            &#x2F;&#x2F;子组件中，默认无法访问到父组件中的data上的数据和methods中的方法</span><br><span class="line">            com1:&#123;</span><br><span class="line">                data()&#123;&#x2F;&#x2F;子组件中的data数据是子组件自己私有的，和父组件无关系</span><br><span class="line">                    return &#123;</span><br><span class="line">                        title:&#39;123&#39;,</span><br><span class="line">                        content:&#39;qqq&#39;</span><br><span class="line">                    &#125;</span><br><span class="line">                 &#125;,</span><br><span class="line">                template:&#39;&lt;h1&gt;这是子组件&#123;&#123;parentmsg&#125;&#125;&lt;&#x2F;h1&gt;&#39;,</span><br><span class="line">                &#x2F;&#x2F; 注意组件中的所有props中的数据，都是通过父组件传递给子组件的</span><br><span class="line">                props:[&#39;parentmsg&#39;],</span><br><span class="line">                &#x2F;&#x2F;注意这是一个数组,父组件传递过来的parentmsg需在props先定义一下这样才能使用</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">     &#x2F;&#x2F; 子组件中的data是可读可写的，props中的数据都是只读的无法重新赋值</span><br><span class="line">&lt;&#x2F;script&gt;</span><br></pre></td></tr></table></figure>

<h5 id="子传父-emit"><a href="#子传父-emit" class="headerlink" title="子传父    $emit()"></a>子传父    $emit()</h5>]]></content>
      <categories>
        <category>Vue</category>
      </categories>
  </entry>
</search>
